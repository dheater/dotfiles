# Web Content Fetching

ALWAYS use `md-fetch` CLI. NEVER use `web-fetch`.

```bash
md-fetch "https://example.com/docs" > output.md
```

Install: `brew install md-fetch` (macOS) / `cargo install md-fetch` (Linux)
